spring:
  application:
    name: qr-file-sharing

  datasource:
    url: jdbc:postgresql://localhost:5432/qr_file_sharing
    username: postgres
    password: aman123

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

server:
  error:
    include-message: always
    include-binding-errors: always

logging:
  level:
    org.springframework: INFO